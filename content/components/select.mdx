---
title: Select
description: React select dropdown component with grouped options, per-item descriptions, multi-select, action items, and column-based layouts
---

import { Select } from "@plexui/ui/components/Select"
import {
  SelectBaseDemo,
  SelectCustomViewsDemo,
  SelectActionsDemo,
  SelectGroupedOptionsDemo,
  SelectMultiSelectDemo,
  SelectColumnConfigDemo,
} from "@/components/docs/SelectDemos"

<UsageBlock>{`import { Select } from "@plexui/ui/components/Select";`}</UsageBlock>

## Examples

### Overview

<ComponentPreview code={`<Select
  value={fruit}
  options={fruits}
  onChange={handleChange}
  placeholder="Select a fruit..."
  variant="solid"
/>`} previewMinHeight={168}>
  <SelectBaseDemo />
</ComponentPreview>

### With descriptions

<ComponentPreview details={<>Options carrying a <code>description</code> field render a secondary line under each label. The title stays semibold in every state (idle, highlighted, selected) — the component handles that automatically, no <code>optionClassName</code> needed. Use <code>TriggerStartIcon</code> to reflect the selected option's identity in the trigger.</>} code={`const RoleOptionDescription = ({ children }: { children: ReactNode }) => (
  <div className="font-normal text-secondary text-[0.935em] leading-[1.45]">
    {children}
  </div>
);

const roles = [
  {
    value: "owner",
    label: "Owner",
    description: (
      <RoleOptionDescription>
        Can modify project information and manage project members
      </RoleOptionDescription>
    ),
  },
  {
    value: "reader",
    label: "Reader",
    description: (
      <RoleOptionDescription>
        Can make API requests that read or modify data
      </RoleOptionDescription>
    ),
  },
];

<Select
  value={role}
  options={roles}
  placeholder="Select role..."
  align="start"
  listMinWidth={260}
  variant="ghost"
  size="lg"
  onChange={({ value }) => setRole(value)}
  TriggerStartIcon={role === "owner" ? UserLock : User}
/>`} previewMinHeight={168}>
  <SelectCustomViewsDemo />
</ComponentPreview>

### Actions

<ComponentPreview details={<>Custom, arbitrary actions can be displayed below the options list via the <code>actions</code> prop.</>} code={`<Select
  value={value}
  onChange={handleChange}
  options={items}
  placeholder="Select..."
  align="start"
  listMinWidth={240}
  actions={[
    { id: "create", label: "Create project", Icon: Plus, onSelect: () => {} },
    { id: "overview", label: "Organization overview", Icon: Workspace, onSelect: () => {} },
  ]}
/>`} previewMinHeight={168}>
  <SelectActionsDemo />
</ComponentPreview>

### Grouped options

<ComponentPreview details={<>Select supports option groups, similar to the native <code>&lt;optgroup&gt;</code> element. All selects are searchable by default, but will only present a searchbox when it makes sense. Currently, this means when there are 15 or more options.</>} code={`<Select
  value={value}
  options={groupedItems}
  onChange={(v) => setValue(v.value)}
  variant="outline"
  size="lg"
  side="bottom"
  listMinWidth={300}
  searchPlaceholder="Select an option..."
  clearable
/>`} previewMinHeight={168}>
  <SelectGroupedOptionsDemo />
</ComponentPreview>

### Multiple selection

<ComponentPreview details={<>Multiple selection is supported through the <code>multiple</code> property. This changes how <code>value</code>, <code>onChange</code>, and <code>TriggerView</code> are typed, accepting and returning arrays of values.</>} code={`<Select
  variant="soft"
  placeholder="Select fruits..."
  options={fruitsOptions}
  name="fruits"
  multiple
  clearable
  value={fruits}
  onChange={(values) => setFruits(values.map(({ value }) => value))}
  TriggerView={MultiFruitTriggerView}
/>`} previewMinHeight={168}>
  <SelectMultiSelectDemo />
</ComponentPreview>

### Column configuration

<ComponentPreview details={<>A column-visibility panel built with <code>Popover</code> and <code>Switch</code>. Toggling a switch moves the column between shown and hidden groups.</>} code={`const [shown, setShown] = useState(['name', 'email', 'role', 'status'])
const toggle = (id) => {
  setShown((prev) =>
    prev.includes(id) ? prev.filter((c) => c !== id) : [...prev, id]
  )
}
const shownColumns = allColumns.filter((c) => shown.includes(c.id))
const hiddenColumns = allColumns.filter((c) => !shown.includes(c.id))
  <Popover.Trigger>
    <Button variant="outline" size="sm"><SettingsSlider /> Columns</Button>
  </Popover.Trigger>
  <Popover.Content minWidth={240} side="bottom" align="start">
    {shownColumns.map((col) => (
      <Row key={col.id} onClick={() => toggle(col.id)}>
        <span>{col.label}</span>
        <Switch checked />
      </Row>
    ))}
    {hiddenColumns.map((col) => (
      <Row key={col.id} onClick={() => toggle(col.id)}>
        <span className="text-tertiary">{col.label}</span>
        <Switch checked={false} />
      </Row>
    ))}
  </Popover.Content>
</Popover>`} previewMinHeight={168}>
  <SelectColumnConfigDemo />
</ComponentPreview>

## Reference

Use `aria-labelledby` with the ID of a visible field label. The default trigger includes that label and its current value or placeholder in its accessible name. `aria-label`, `aria-describedby` and `aria-invalid` also reach the default trigger. Custom triggers manage their own accessibility attributes.

Searchable menus name their input `Search options` by default. Set `searchAriaLabel` for a more specific search label without changing the trigger's accessible name or the visible placeholder.

<PropsTable data={[
  { prop: "options", type: "Options<T>", description: "Array of options or grouped options" },
  { prop: "value", type: "string | string[]", description: "Selected value (single) or values (multiple)" },
  { prop: "onChange", type: "function", description: "Called when selection changes" },
  { prop: "multiple", type: "boolean", default: "false", description: "Allow multiple selection" },
  { prop: "placeholder", type: "string", default: '"Select..."', description: "Trigger placeholder" },
  { prop: "variant", type: "string", default: '"outline"', description: "Trigger variant" },
  { prop: "size", type: "string", default: '"md"', description: "Trigger size" },
  { prop: "pill", type: "boolean", default: "false", description: "Rounded trigger" },
  { prop: "clearable", type: "boolean", default: "false", description: "Show clear button" },
  { prop: "block", type: "boolean", default: "true", description: "Full width trigger" },
  { prop: "disabled", type: "boolean", default: "false", description: "Disable select" },
  { prop: "actions", type: "array", description: "Actions below options list" },
  { prop: "checkPosition", type: '"start" | "end" | "none"', default: '"start"', description: 'Where the check indicator sits in the list. "none" removes the indicator column for lists that never carry a selection' },
  { prop: "OptionView", type: "React.FC", description: "Custom option render" },
  { prop: "TriggerView", type: "React.FC", description: "Custom trigger render (multi)" },
  { prop: "searchPlaceholder", type: "string", description: "Search input placeholder" },
  { prop: "searchAriaLabel", type: "string", default: '"Search options"', description: "Accessible name of the search input, independent of the trigger label" },
]} />

Select shares the unified 9-step size scale with Button, Input, SelectControl, and SegmentedControl.
