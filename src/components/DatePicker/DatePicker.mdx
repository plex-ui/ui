import { ArgTypes, Controls, Meta, Subtitle, Title } from '@storybook/blocks';
import { CollapsibleCanvas, CollapsibleSource } from '@storybookComponents/CollapsibleCanvas';
 
import * as DatePickerStories from './DatePicker.stories';
 
<Meta of={DatePickerStories} />
 
<Title>Date Picker</Title>
<Subtitle>Select a single date with an interactive calendar</Subtitle>

## Usage
```jsx
import { DatePicker } from "@plexui/ui/components/DatePicker";
```

<CollapsibleCanvas of={DatePickerStories.Base}/>
<CollapsibleSource of={DatePickerStories.Base} />

## Reference
<ArgTypes of={DatePickerStories.Base} />

## Examples

### Limits

Use `min` and `max` to limit selectable dates to a specific timeframe.

<CollapsibleCanvas of={DatePickerStories.Limits}/>
<CollapsibleSource of={DatePickerStories.Limits} />

### Without Icon

Remove the calendar icon from the trigger by setting `triggerShowIcon` to false.

<CollapsibleCanvas of={DatePickerStories.WithoutIcon} />
<CollapsibleSource of={DatePickerStories.WithoutIcon} />

### Disabled

Disable the picker to prevent user interaction.

<CollapsibleCanvas of={DatePickerStories.Disabled}/>
<CollapsibleSource of={DatePickerStories.Disabled} />
