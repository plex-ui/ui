import { ArgTypes, Controls, Meta, Subtitle, Title } from '@storybook/blocks';
import { CollapsibleCanvas, CollapsibleSource } from '@storybookComponents/CollapsibleCanvas';
import * as CheckboxStories from './Checkbox.stories';
 
<Meta of={CheckboxStories} />
 
<Title>Checkbox</Title>
<Subtitle>Toggle control for on and off states</Subtitle>

## Usage
```jsx
import { Checkbox } from "@plexui/ui/components/Checkbox";
```

<CollapsibleCanvas of={CheckboxStories.Base} />
<CollapsibleSource of={CheckboxStories.Base} />

## Reference
<ArgTypes of={CheckboxStories.Base} />

## Examples

### Indeterminate

Use `checked="indeterminate"` to indicate that the checkbox is partially selected.

<CollapsibleCanvas of={CheckboxStories.Indeterminate} />
<CollapsibleSource of={CheckboxStories.Indeterminate} />
