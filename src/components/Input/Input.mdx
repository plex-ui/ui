import { Controls, ArgTypes, Meta, Subtitle, Title } from '@storybook/blocks';
import { CollapsibleCanvas, CollapsibleSource } from '@storybookComponents/CollapsibleCanvas';
import * as InputStories from './Input.stories';
 
<Meta of={InputStories} />
 
<Title>Input</Title>
<Subtitle>Semantic input text collection</Subtitle>

## Usage
```jsx
import { Input } from "@plexui/ui/components/Input";
```

<CollapsibleCanvas of={InputStories.Base} />
<CollapsibleSource of={InputStories.Base} />

## Reference

<ArgTypes of={InputStories.Base} exclude={['ref']} />


## Examples

### Variants
Use `variant` to stylize the input.

<CollapsibleCanvas of={InputStories.Variants} />

### Sizing & roundness

Sizing of the input is controlled with `size` and `gutterSize` props. `size` sets the input to a specific control `height`, and `gutterSize` sets the horizontal padding.

Each `size` provides default values for `gutterSize`, as well as `font-size`. Each of these values can be overridden independently.

<CollapsibleCanvas of={InputStories.Sizing} />
<div className="canvas-controls-only">
<Controls of={InputStories.Sizing}/>
</div>


### Start adornment

You can add arbitrary content to the beginning of an input with `startAdornment` &ndash; like a search icon.

Ideally, the spacing will look pretty good by default. If you need to customize the spacing:
- `gutterSize` determines the amount of spacing on the edges of the container, which you can fine-tune as needed.
  - If you need precise spacing not supported by `gutterSize`, use negative left margin on your adornment directly (e.g., `-ml-2`).
- The spacing between the adornment and the input is controlled with `gap` on the container, which you can customize as needed with Tailwind classes.

<CollapsibleCanvas of={InputStories.StartAdornment} />
<CollapsibleSource of={InputStories.StartAdornment} />

### Clear button

Use the `onClear` prop to display a built-in clear button. The button appears when the input has a value and is not disabled or read-only.

<CollapsibleCanvas of={InputStories.EndAdornment} />
<div className="canvas-controls-only">
<Controls of={InputStories.EndAdornment}/>
</div>
<CollapsibleSource of={InputStories.EndAdornment} />

### End adornment

You can add arbitrary content to the end of an input with `endAdornment` &ndash; like a character counter or custom icon.

The same spacing guidance from `startAdornment` applies here.

### Disabled

Use the `disabled` prop to disable the input, greying it out and preventing user interactions.

<CollapsibleCanvas of={InputStories.Disabled} />
<div className="canvas-controls-only">
<Controls of={InputStories.Disabled}/>
</div>
<CollapsibleSource of={InputStories.Disabled} />

### Invalid

Use the `invalid` prop to mark the input as invalid. You decide how this state is removed, whether that's through `onFocus`, `onChange`, an `onSubmit` handler, etc.

Default visual styles will be applied, based on the `variant`. Additionally, `[data-invalid]` is applied to the container for custom styling.

<CollapsibleCanvas of={InputStories.Invalid} />
<div className="canvas-controls-only">
<Controls of={InputStories.Invalid}/>
</div>
<CollapsibleSource of={InputStories.Invalid} />

### Auto select text

Pass `autoSelect` to select all text in the input &ndash; useful when pairing with other copy actions. This prop behaves like an imperative action when toggled and allows selection to change.

<CollapsibleCanvas of={InputStories.AutoSelect} />
<div className="canvas-controls-only">
<Controls of={InputStories.AutoSelect}/>
</div>
<CollapsibleSource of={InputStories.AutoSelect} />

### Autofill extensions

By default, autofill extensions are prevented by `Input`. For the occasions where you want browser extensions to be present, pass `allowAutofillExtensions` to remove the disabling.

When `name` is passed, or `type` is set to `"password"`, this value will default to `true`.

<CollapsibleCanvas of={InputStories.AutofillExtensions} />
<div className="canvas-controls-only">
<Controls of={InputStories.AutofillExtensions}/>
</div>
<CollapsibleSource of={InputStories.AutofillExtensions} />

### With button

You can pair `Input` and `Button` components together by using the same `size` value.

<CollapsibleCanvas of={InputStories.WithButton} />
<div className="canvas-controls-only">
<Controls of={InputStories.WithButton}/>
</div>
<CollapsibleSource of={InputStories.WithButton} />

### Optical alignment

Use `opticallyAlign` with either `"start"` or `"end"` to offset the gutter of the input.

<CollapsibleCanvas of={InputStories.OpticalAlignment} />
