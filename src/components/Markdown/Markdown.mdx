import { ArgTypes, Meta, Subtitle, Title } from "@storybook/blocks"
import { CollapsibleCanvas, CollapsibleSource } from '@storybookComponents/CollapsibleCanvas';

import * as MarkdownStories from "./Markdown.stories"

<Meta of={MarkdownStories} />

<Title>Markdown</Title>
<Subtitle>Render rich formatted content</Subtitle>

## Usage
```jsx
import { Markdown } from "@plexui/ui/components/Markdown"
```

<CollapsibleCanvas of={MarkdownStories.Base} layout="padded" />
<CollapsibleSource of={MarkdownStories.Base} />

## Reference
<ArgTypes of={MarkdownStories.Base} />

## Math

Use `includeMath` to enable LaTeX rendering.

<CollapsibleCanvas of={MarkdownStories.Math} layout="padded" />
<CollapsibleSource of={MarkdownStories.Math} />
